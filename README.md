My personal blog on data science topics.

## Local development

### First-time setup

```bash
gem install bundler
bundle install
```

### Preview the site

```bash
bundle exec jekyll serve
```

Then open http://localhost:4000. The site auto-rebuilds when you save files.
